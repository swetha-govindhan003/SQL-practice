CREATE TABLE STUDENT(
  STUDENT_id INT,
  dept_id INT,
  STUDENT_name VARCHAR,
  STUDENT_marks INT
);

INSERT INTO STUDENT(STUDENT_id,dept_id,STUDENT_name,STUDENT_marks)
VALUES
(1,101,'joe', 95),
(2,102,NULL, 0),
(3,103,'geography',87);


CREATE TABLE dept(
  dept_id INT,
  dept_name VARCHAR,
  rank INT
);
INSERT INTO dept(dept_id, dept_name,rank)
VALUES
(1,'comp.science', 1),
(3,'maths', 11),
(4,'geography',2);

SELECT STUDENT.STUDENT_id, dept.dept_name
FROM STUDENT LEFT OUTER JOIN dept
ON STUDENT.STUDENT_id=dept.dept_id;
